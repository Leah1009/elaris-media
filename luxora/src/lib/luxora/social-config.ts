/**
 * Luxore's own social profiles (not a business's — those live on each
 * business's website settings). Empty until real profiles exist; the
 * footer hides any link left empty rather than pointing at a generic
 * platform homepage.
 */
export const SOCIAL_CONFIG = {
  instagramUrl: "",
  facebookUrl: "",
  tiktokUrl: "",
};
